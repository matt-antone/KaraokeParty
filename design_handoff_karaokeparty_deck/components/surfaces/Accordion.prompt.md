Collapsible group inside a Panel — one per media folder, one per player preference group.

```jsx
<Accordion heading="Media Folders" initialExpanded>…</Accordion>
```

The header is a raised key sitting in a recessed well, so open and closed both read as mechanical. The chevron rotates 90 degrees; it does not swap for a down-chevron. Content is padded by the component.
