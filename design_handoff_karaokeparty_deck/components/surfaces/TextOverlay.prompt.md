Every empty state: Queue Empty, Nothing Queued, Nothing Sung Yet, Library Empty, Get a Room.

```jsx
<TextOverlay title="Queue Empty">
  Tap a song in the <a href="#library">library</a> to queue it.
</TextOverlay>
```

Fixed shape: silkscreen headline naming the state, then one sentence pointing at the fix with the destination as an inline amber link. No illustration, no key, no second paragraph.
