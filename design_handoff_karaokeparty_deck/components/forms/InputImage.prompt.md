Singer photo, captured when an account is created and editable afterwards. It matters: the photo is how people find their turn in the queue and who the player announces at intermission.

```jsx
<InputImage src={preview} onChange={setFile} onClear={clear} />
```

Always square — avatars are square everywhere. Sits left of the name field in a row with `gap: var(--gap-3)`.
