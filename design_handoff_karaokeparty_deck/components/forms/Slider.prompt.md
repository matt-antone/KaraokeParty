Linear fader for the Display modal's settings: lyrics size, lyrics background, visualizer sensitivity, QR size.

```jsx
<Slider min={0.4} max={0.9} value={size} onChange={setSize} label="Lyrics size" />
```

Travelled section is amber, remaining track is a recessed well, cap is a machined key. Label it with a silkscreen `<label>` above; there is no numeric readout anywhere in the product. Room volume uses a `Knob`, not this — keep the distinction.
