The only loading indicator: a VU meter drifting at low level, centred in whatever box you give it.

```jsx
{isLoading && <Spinner />}
```

Never reaches peak and never lights red — it's idle signal, not activity. No spinner rings, no skeletons, no loading copy anywhere in the product.
